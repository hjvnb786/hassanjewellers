import 'package:flutter/material.dart';
import 'package:hassanjewellers/Screens/join_new_scheme.dart';

class NewScheme extends StatelessWidget {
  const NewScheme({super.key});

  @override
  Widget build(BuildContext context) {
    // Return the new multi-step form directly
    return const JoinNewScheme();
  }
}
